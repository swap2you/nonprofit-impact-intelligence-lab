# Security and privacy

This demo has no secrets, credentials, PII, donor identities, outbound messaging, or production deployment. `.env` files and `00_INBOX/` are ignored. API exports contain synthetic aggregates. For production, add authentication, authorization, encrypted transport/storage, audit retention, secret management, row-level access controls, and formal privacy review.
